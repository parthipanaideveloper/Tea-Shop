rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all collections (shops, global_categories, franchise_owners, templates, etc.)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
